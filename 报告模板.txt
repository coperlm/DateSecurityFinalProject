隐私保护的联邦学习框架
1 xxxx
1.1 xxxxx
在发送的ClientHello中会带上自己支持的加密算法，供服务端从中挑选。由于老旧客户端会支持一些不安全的加密算法，为了提高传输安全，通常会在服务端指定一个可用算法列表，最终使用的加密类型取决于二者的交集，并按服务端优先级取第一个；如果没有交集，直接终止会话。在Nginx中这个功能通过将ssl_prefer_server_ciphers设置为on开启。

